const registerView = (req, res) => {
  res.render("../views/login", {});
};

const loginView = (req, res) => {
  res.render("../views/login", {});
};

module.exports = {
  registerView,
  loginView,
};
